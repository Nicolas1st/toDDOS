type siteMetadataConfig = {
  name: string,
  title: string,
}

type config = {siteMetadata: siteMetadataConfig}

let config = {
  siteMetadata: {
    name: "toDDOS app",
    title: `Yet another toDDOS app`,
  },
}
